# ============================================================================
# Deployment Exclusion List
#
# This file specifies which files should NOT be synced to the production server
# when deploying with rsync (used by deploy-from-local.sh)
# ============================================================================

# Git and version control
.git/
.github/
.gitignore
.gitattributes

# Editor and IDE
.vscode/
.idea/
*.swp
*.swo
*~

# Logs
*.log
npm-debug.log*
yarn-debug.log*
yarn-error.log*
lerna-debug.log*

# Documentation (keep README.md)
*.md
!README.md

# Environment files (CRITICAL - never deploy local env files)
.env.local
.env.development
.env.test
.env*.local

# Dependencies - will be installed on server with --production flag
node_modules/

# Source files - Next.js uses compiled .next folder
# Keep middleware and instrumentation as they might be needed
src/
!src/middleware.ts
!src/instrumentation.ts
!src/instrumentation.js

# TypeScript config and source
tsconfig.json
tsconfig.*.json

# Test files and coverage
__tests__/
*.test.ts
*.test.tsx
*.test.js
*.test.jsx
*.spec.ts
*.spec.tsx
*.spec.js
*.spec.jsx
coverage/
.nyc_output/

# Next.js build cache (regenerated on server)
.next/cache/
.next/trace

# Build artifacts that can be regenerated
.turbo/
.eslintcache
tsconfig.tsbuildinfo

# OS files
.DS_Store
.DS_Store?
._*
.Spotlight-V100
.Trashes
ehthumbs.db
Thumbs.db
desktop.ini

# Deployment scripts (not needed on server)
deploy-from-local.sh
DEPLOYMENT.md
DEPLOY_QUICK_START.md

# Local development tools
.prettierrc
.prettierignore
.eslintrc.json
.eslintignore

# Temporary files
tmp/
temp/
*.tmp

# Docker (if not using Docker on server)
Dockerfile
.dockerignore
docker-compose.yml

# CI/CD (if any)
.travis.yml
.gitlab-ci.yml
.circleci/

# Storybook
.storybook/
storybook-static/

# Prisma (schema is in backend, not frontend)
prisma/
