# Fix Bugs

Find and fix bugs in the following area: $ARGUMENTS

## Steps:

1. **Search for relevant files** - Use Glob and Grep to find files related to "$ARGUMENTS"
2. **Identify potential issues** - Read the code and look for:
   - Missing error handling
   - Incorrect API calls
   - State management issues
   - Type errors
   - Logic bugs
3. **Fix the bugs** - Make the necessary code changes
4. **Run build to verify** - Execute `npm run build` to ensure no compilation errors

## Guidelines:
- Focus only on the area specified: $ARGUMENTS
- Don't introduce new features, just fix bugs
- Maintain existing code style
- Add error handling where missing
- Test the fix if possible
