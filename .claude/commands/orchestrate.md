# Orchestrate Agents

Use the Task tool to delegate this work to specialized agents: $ARGUMENTS

## Instructions

1. Break the task into sub-tasks
2. Launch multiple agents in parallel using a single message with multiple Task tool calls
3. Use appropriate agent types: `Explore`, `Plan`, `frontend-ui`, `backend-api`, `systems-architect`, `qa`, `documentation`, `general-purpose`
4. Summarize results when agents complete

Execute now.
